import React from 'react'
import classNames from 'classnames/bind'

import style from 'elements/Line.scss'

const cx = classNames.bind(style)

const Line = () => {
  return <div className={cx('Line')} />
}

export default Line
